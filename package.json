{
  "name": "@inpageedit/__root__",
  "private": true,
  "version": "0.0.0",
  "description": "🚀 Modular, Extensible Supercharged Plugin for MediaWiki",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "setup-dev": "zx scripts/setup-dev.ts",
    "dev": "npm run core:dev",
    "build": "npm run core:build",
    "core:dev": "pnpm --filter core dev",
    "core:build": "pnpm --filter core build",
    "docs:dev": "pnpm --filter docs dev",
    "docs:build": "pnpm --filter docs build"
  },
  "devDependencies": {
    "@prettier/plugin-pug": "^3.4.2",
    "@types/node": "^22.18.12",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "zx": "^8.8.5"
  }
}